using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundDetection : MonoBehaviour
{
    //public AudioSource audioSource; // Reference to the AudioSource.
    //public string targetSoundName; // Name of the sound to detect.
    //public Animator animator; // Reference to the Animator component.

    //private Detector detector;

    //void Start()
    //{
    //    detector = new Detector(audioSource.clip); // Initialize the audio analysis library with the audio clip.
    //    detector.SetDetectionCallback(DetectionCallback);
    //}

    //void Update()
    //{
    //    detector.Run(); // Run audio analysis in real-time.
    //}

    //// Callback function called when the target sound is detected.
    //void DetectionCallback(float timestamp, float duration)
    //{
    //    Debug.Log("Target sound detected at timestamp: " + timestamp);

    //    // Trigger the animation.
    //    animator.SetTrigger("PlayAnimation");
    //}
}
